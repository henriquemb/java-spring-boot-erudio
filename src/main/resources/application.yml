server:
  port: 8080
cors:
  originPatterns: http://localhost:8080
file:
  upload-dir: /Cursos/java-spring-boot-erudio/uploads
spring:
  application:
    name: java-spring-boot-erudio
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    #Por ser ambiente de estudo não irei usar variável de ambiente :D
    url: jdbc:mariadb://localhost:3306/spring_boot_erudio?useTimezone=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB
logging:
  level:
    root: info
    sql: info
    com.github.henriquemb: info
springdoc:
  paths-to-match: /api/v1/**
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    use-root-path: true